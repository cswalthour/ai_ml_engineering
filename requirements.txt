# Core runtime
snowflake-snowpark-python==1.30.0
pandas>=2.0,<3
python-dotenv>=1.0,<2
anthropic>=0.35.0,<1

# Streamlit UI (local)
streamlit==1.44.1
streamlit-extras==0.4.0

# Needed for local execution due to `from snowflake.ml import *` imports in repo modules.
snowflake-ml-python>=1.7,<2

